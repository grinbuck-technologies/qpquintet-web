/** The prefers-reduced-motion media query string, shared by every component that checks it directly (rather than relying solely on the CSS-level media query in globals.css). */
export const REDUCED_MOTION_QUERY = "(prefers-reduced-motion: reduce)";
